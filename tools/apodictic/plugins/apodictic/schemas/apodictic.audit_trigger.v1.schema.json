{
  "$id": "apodictic.audit_trigger.v1",
  "$comment": "Source of truth for the apodictic:audit_trigger block. Validated by scripts/apodictic_artifacts.py (stdlib subset checker). Closed-key (additionalProperties:false) enforced by the engine (Harness Contracts v2): a misspelled field now fails instead of validating clean. Declared closed in schemas/_coverage.json.",
  "title": "APODICTIC audit trigger (apodictic.audit_trigger.v1)",
  "type": "object",
  "required": ["schema", "audit", "evidence", "recommendation"],
  "additionalProperties": false,
  "properties": {
    "schema": {"const": "apodictic.audit_trigger.v1"},
    "audit": {"type": "string"},
    "evidence": {"type": "string"},
    "recommendation": {"enum": ["run", "recommend", "defer"]}
  }
}
