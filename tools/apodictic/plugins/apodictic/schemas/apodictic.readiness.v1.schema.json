{
  "$id": "apodictic.readiness.v1",
  "$comment": "Source of truth for the apodictic:readiness block. Validated by scripts/apodictic_artifacts.py (stdlib subset checker). Closed-key (additionalProperties:false) enforced by the engine (Harness Contracts v2): a misspelled field now fails instead of validating clean. Declared closed in schemas/_coverage.json.",
  "title": "APODICTIC readiness verdict (apodictic.readiness.v1)",
  "type": "object",
  "required": ["schema", "dimension", "verdict", "rationale"],
  "additionalProperties": false,
  "properties": {
    "schema": {"const": "apodictic.readiness.v1"},
    "dimension": {"type": "string"},
    "verdict": {"type": "string"},
    "rationale": {"type": "string"}
  }
}
