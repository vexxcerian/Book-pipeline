# Findings Ledger — *The Tidewater Braid* (intake-interview fixture)

*A minimal fixture Findings Ledger carrying a structured, detected-but-unsettleable ambiguity — one
`LOW`-confidence timeline finding (`F-P2-04`) the framework cannot resolve from the text alone — plus
one `### Unresolved Questions` bullet with no id scheme. It exists only to anchor the worked Intake
Interview so the canonical `intake-interview` `--check-all` gate has real provenance to resolve
(`I3`): `F-P2-04` for the referential path, the UQ bullet for the prose `source_note` path. Not a
full Ledger.*

### Notable Findings

- A LOW-confidence Pass 2 reading: the non-linear chapter ordering may be a deliberate braid, or it
  may be drift — the text does not settle which, and only the author knows.

   <!-- apodictic:finding
   {
     "schema": "apodictic.finding.v1",
     "id": "F-P2-04",
     "mechanism": "the non-linear ordering across Chapters 4-6 may read as a deliberate braided timeline or as unsignalled drift — there is no in-text marker establishing the reordering as intentional",
     "severity": "Should-Fix",
     "confidence": "LOW",
     "evidence_refs": ["Ch 4", "Ch 5", "Ch 6"],
     "fix_class": "if intended, add a structural signal that flags the braid; if not, restore chronological order",
     "risk_if_fixed": "an over-signalled braid could feel mechanical; restoring order could flatten a deliberate effect"
   }
   -->

### Unresolved Questions

- Does the second-person address in the interludes read as a deliberate device, or as an artifact of an earlier draft the author meant to revise out?
