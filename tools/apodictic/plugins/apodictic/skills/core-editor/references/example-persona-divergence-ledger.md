# Findings Ledger — *The Tidewater Braid* (persona-divergence fixture)

*A minimal fixture Findings Ledger carrying one LOCKED finding (`F-P1-04`, Must-Fix / HIGH) so the
canonical `persona-divergence` `--check-all` gate has a real anchor to resolve (`D2`) and a locked
severity to anchor against (`D3`). Not a full Ledger; it exists only to anchor the worked Persona
Divergence Map.*

### Notable Findings

- A Pass-1 reader-dynamics finding: a three-scene lull in Chapter 3 with no tension beat.

   <!-- apodictic:finding
   {
     "schema": "apodictic.finding.v1",
     "id": "F-P1-04",
     "mechanism": "three consecutive scenes in Chapter 3 run under the chapter's median word count with no tension beat, flattening the pace across the act's midpoint",
     "severity": "Must-Fix",
     "confidence": "HIGH",
     "evidence_refs": ["Ch 3 §1", "Ch 3 §2", "Ch 3 §3"],
     "fix_class": "introduce a tension beat or compress one of the three lull scenes",
     "risk_if_fixed": "an added beat could overstate a deliberately quiet passage"
   }
   -->
