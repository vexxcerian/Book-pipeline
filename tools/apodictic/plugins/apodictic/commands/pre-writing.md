---
description: Guide a writer from idea to draftable structure (no manuscript required)
allowed-tools: Read, Write, Edit, Bash, Glob
---

Alias shortcut for `/start` with prefilled router values:

- `artifact=idea`
- `goal=draft`
- `concern=general`

Load `../skills/pre-writing-pathway/SKILL.md` and run the normal pathway from Phase 0 through Phase 6. If router output is already available, consume it as prefilled values and skip redundant intake questions.

Keep output and re-entry behavior identical to `/start`-routed pre-writing:

- Build Structural Plan / Minimal Viable Plan
- Persist outputs and rolling state files to the active project output context beside the manuscript, never to the plugin repo
- Provide re-entry path back to core diagnostic once a draft exists

If a project name is provided as text, use it for the project directory and file naming.
